'use strict';

/**
 * vendor-description controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vendor-description.vendor-description');
